export enum Routes {
  HOME = "/",
  SETTINGS = "/settings",
}

export enum FetchStatus {
  Idle = "idle",
  Loading = "loading",
  Success = "success",
  NotFound = "not-found",
  Failure = "failure",
}
